# 운영 담당자 주의사항

## 개인정보와 분석

상담·자료 신청 API는 상담 처리에 필요한 입력값을 전송하지만, GTM 성공 이벤트는 회사명·담당자명·연락처·이메일을 포함하지 않습니다. 분석 이벤트에는 서비스 유형·지역·예상 식수·폼 유형과 성공·실패 상태만 사용하도록 회귀 테스트가 고정되어 있습니다. 운영 계정에서 별도 사용자 속성을 추가할 경우 개인정보 처리 기준과 동의 범위를 먼저 확인해야 합니다.

## 외부 분석 설정

프로젝트에는 GTM 스니펫과 앱 이벤트 호출이 포함되어 있지만 GA4 태그·키 이벤트, Meta Pixel, Microsoft Clarity의 실제 컨테이너 설정과 운영 Preview 검증은 외부 계정 권한이 필요합니다. 해당 계정에서 이벤트 수신을 확인하기 전에는 외부 분석 통합을 완료로 표시하지 않습니다. 상세 이벤트명은 `ANALYTICS_EVENT_MAP.md`를 기준으로 합니다.

## Power Automate

Power Automate 연동은 실제 `POWER_AUTOMATE_WEBHOOK_URL`이 제공되기 전까지 `not_configured` 상태로 유지해야 합니다. URL을 받은 뒤에만 환경 변수 등록, 상담 신청 성공 시 Webhook 호출, 실패 처리, 관리자 상태 표시를 검증합니다. URL이나 토큰을 소스 코드·문서에 직접 기록하지 않습니다.

## 배포와 체크포인트

배포는 최신 체크포인트를 먼저 저장한 뒤 Management UI의 Publish 흐름에서 수행합니다. 운영 도메인과 API, SSL, 관리자 세션, 외부 분석 Preview는 실제 운영 계정에서 별도로 확인해야 하며, 이 샌드박스의 개발 미리보기 검증만으로 운영 성공을 단정하지 않습니다.
