
## 2026-08-27 후속 이미지·필터 작업 시각 검증
- 데스크톱 `/`: 메인 배너의 상담받기·자료 다운받기 버튼이 동일한 흰색 On-brand 스타일로 렌더링됨.
- 모바일 `/` (375×812): 두 CTA가 동일한 높이·radius·색상 체계로 가운데 정렬되어 표시되며, 제목·설명·네비게이션이 기존 반응형 레이아웃을 유지함.
- `/admin`: 관리자 로그인 화면 정상 렌더링 확인.
- 기간 필터는 로그인 이후 대시보드에서 날짜 입력 2개와 기존 검색·상태·서비스·정렬 필터가 함께 표시되도록 구현됨.
- 개발 서버 TypeScript 상태는 오류 없음. 콘솔에는 기존 DB 인증 오류 로그가 남아 있으나 프론트엔드 컴파일 오류는 확인되지 않음.

## CTA 기본 GREEN700·hover 화이트 검증
- 데스크톱 홈페이지 hero에서 상담받기·자료 다운받기 버튼이 모두 GREEN700 계열 배경으로 표시됨.
- 모바일 홈페이지 hero에서도 두 버튼이 동일한 크기·간격·반응형 정렬로 표시됨.
- hover 상태는 공통 `on-brand` variant의 `hover:bg-white hover:border-primary hover:text-primary` 클래스와 버튼 회귀 테스트로 검증함.

## 스낵픽 실패 생성 에셋 점검
- 저장소 응답을 확인한 결과 여러 `snackpick-*.jpg` 경로가 실제 이미지가 아니라 `Image generation failed` SVG(498 bytes)를 반환함.
- 유효한 기존 에셋으로 확인된 `pasted_file_5hIjDd_image_ccf1f4d9.png`는 포장 간식 제품 이미지이고, `cropped_cafe_IMG_6844_dd3c1ce0.jpg`는 카페 운영 사진임.
- 실패한 이미지 경로는 실제 유효 에셋으로 교체하기 전까지 완료 처리하지 않음.

## 유효 이미지 후보 추가 검토
- `snackpick-ramen-reference_d9f27046.jpg`는 1920×1920 정사각형 라면 제품 사진으로 카드용 대체 에셋으로 사용 가능함.
- `snackpick-solution_634d0e45.webp`는 스낵픽 진열·사내 공간 운영 사진으로 섹션 대표 이미지에 적합함.

## 스낵픽 최종 에셋 연결 확인
- 실패 SVG를 반환하던 생성 경로는 Home.tsx의 상품 데이터에서 제거함.
- 현재 연결된 고유 에셋은 라면 정사각형 제품 사진, 포장 간식 이미지, 카페 운영 사진, 핑거푸드 사진이며 모두 저장소에서 HTTP 200과 이미지 콘텐츠로 응답함.
- 남은 `snackpick-solution_634d0e45.webp`는 스낵픽 운영 공간 대표 이미지로 유효함.

## 스낵픽 PC·모바일 시각 검증
- 데스크톱 전체 페이지에서 스낵픽 상품 카드가 깨진 실패 SVG 없이 정사각형 이미지 카드로 표시됨.
- 모바일 전체 페이지에서도 카테고리 4×2 그리드와 상품 카드가 정상적으로 쌓이며, 교체된 이미지가 렌더링됨.
- 고객 성공사례·하단 CTA·푸터까지 기존 반응형 흐름을 유지함.

## 검색 접근성 기본 파일 검증
`client/public/robots.txt`를 추가해 공개 랜딩 경로는 허용하고 `/admin`, `/api` 경로는 검색 크롤링에서 제외했습니다. 정적 파일 존재 여부, 관리자 경로 차단 지시문, SEO 테스트, TypeScript 검사, 프로덕션 빌드를 확인했습니다.

## 최신 런타임 점검
최근 브라우저 콘솔에서 error·uncaught·failed 항목이 발견되지 않았고, 네트워크 로그에도 4xx·5xx 실패 요청이 없었습니다. 공통 `on-brand` variant는 기본 `bg-primary`와 `text-primary-foreground`를 사용하고 hover 시 `bg-white`, `border-primary`, `text-primary`로 전환됩니다.

## 관리자 기간 필터 반응형 확인
모바일 `/admin/login`과 `/admin/dashboard` 경로를 확인했습니다. 인증되지 않은 `/admin/dashboard` 접근은 관리자 로그인 화면으로 안전하게 리다이렉트되므로, 실제 기간 필터 화면의 모바일 렌더링은 관리자 세션 없이는 시각 검증할 수 없습니다. 기간 필터의 반응형 클래스와 입력 제약은 소스 및 타입 검사로 확인했지만, 인증 후 브라우저 검증 항목은 보류합니다.

## FO 전용 전체 화면 검증
관리자 화면은 제외하고 FO `/` 랜딩페이지를 1280px 데스크톱과 390px 모바일 전체 길이로 확인했습니다. 메인·하단 CTA는 배너 중앙에 배치되고, 모바일에서는 두 CTA가 함께 보이는 컴팩트한 가로 배치가 유지됩니다. 스낵픽 상품 이미지와 카테고리 영역, 고객 성공사례·푸터까지 수평 넘침 없이 렌더링되었습니다. 관리자 기간 필터는 이번 검증 범위에서 제외했습니다.

## 모바일 네비게이션 3열 검증
모바일 FO 네비게이션을 `grid-cols-3`으로 변경해 6개 메뉴가 첫 행 3개·둘째 행 3개로 정렬되는 것을 확인했습니다. 기존 4개·2개 불균형 배치가 제거되었고 타일의 아이콘·텍스트·활성 상태는 유지됩니다. 데스크톱에서는 기존 `grid-cols-6` 구조와 CTA·Hero 레이아웃이 그대로 유지됩니다.

## FO 상담·자료 신청 폼 Text input 가이드 검증
상담받기와 자료 다운받기 CTA가 공통으로 여는 `ConsultationModal`의 입력 요소를 동일한 Text input 토큰으로 정리했습니다. 라벨·필수 표시·helper·오류 메시지의 계층과 `aria-describedby` 연결을 적용했고, Input·Textarea·SelectTrigger에 모바일/데스크톱 크기, 기본·focus·disabled·success·error 상태 토큰을 반영했습니다. 활성 FO 폼의 이전 인라인 입력 스타일 잔여 여부를 정적 검색으로 확인했으며, FO 테스트와 프로덕션 빌드를 통과했습니다.

## Text input 변경 후 FO 반응형 회귀
Text input 가이드 토큰 반영 이후 FO 데스크톱(1280px)과 모바일(375px) 루트 화면을 확인했습니다. 메인 배너 텍스트·CTA 정렬, 모바일 3열 네비게이션의 상단 노출, 기존 hero 레이아웃이 깨지지 않았습니다. 상담 폼 자체의 입력 상태는 관련 컴포넌트 테스트·타입 검사·프로덕션 빌드로 검증했습니다.

## Badge 가이드 적용 후 FO 반응형 검증
Badge 컴포넌트에 StatusBadge의 중립·긍정·주의·경고 상태와 PromoBadge의 filled/outlined 색상 프리셋, large/medium/small 크기를 추가했습니다. FO 카드의 기존 인라인 태그는 PromoBadge green outlined small로 교체했습니다. 데스크톱과 모바일 루트 화면에서 hero·CTA·네비게이션 레이아웃이 유지되는 것을 확인했으며, Badge·폼 관련 회귀 테스트와 TypeScript·프로덕션 빌드를 통과했습니다.

## PromoBadge 예시 스타일 적용 검증
PromoBadge outlined를 첨부 예시처럼 둥근 사각형(`rounded-md`)·화이트 배경·옐로 상태 테두리·짙은 텍스트 조합으로 변경하고, FO 카드 태그의 색상을 yellow outlined small로 적용했습니다. PC와 모바일 전체 페이지 캡처에서 스낵픽·식단·사내카페 태그가 동일한 외형으로 표시되고 카드 레이아웃이 유지되는 것을 확인했습니다.

## FO PromoBadge filled pill 재적용 검증
첨부 예시 기반으로 FO 카드 태그를 `yellow`·`filled`·`small` PromoBadge로 전환했습니다. `#FF9700` 경고색 단색 배경, 짙은 텍스트, 테두리 없는 `rounded-full` pill 형태가 적용됩니다. PC·모바일 전체 화면에서 식단·사내카페·키친리스 카드 태그의 외형과 카드 레이아웃을 확인했으며, Badge·폼 회귀 테스트 13개, TypeScript와 프로덕션 빌드를 통과했습니다.

## FO PromoBadge 최신 아웃라인 복원 검증
최신 첨부 예시에 맞춰 FO 카드 태그를 yellow outlined small로 복원했습니다. 화이트 배경, `#FF9700` 얇은 테두리, 짙은 텍스트, `rounded-md` 둥근 사각형을 유지합니다. PC·모바일 전체 화면에서 식단·사내카페·키친리스 카드의 태그 표시와 기존 카드 레이아웃을 확인했으며, Badge·폼 회귀 테스트 13개, TypeScript·프로덕션 빌드를 통과했습니다.

## FO 식단·사내카페 카드 배경 흰색 통일 검증
식단과 사내카페 카드의 `bg-gray-50` 본체 배경을 키친리스 밀솔루션 카드와 같은 `bg-white`로 변경했습니다. 모바일 카드 내부 이미지 영역의 `bg-gray-100`도 `bg-white`로 통일해 카드 전체의 배경 대비를 맞췄습니다. PC·모바일 전체 화면에서 카드 본체와 이미지 영역이 흰색으로 표시되고 태그·그림자·레이아웃이 유지되는 것을 확인했습니다. 관련 회귀 테스트 15개, TypeScript와 프로덕션 빌드를 통과했습니다.

## FO 상담 신청 폼 Checkbox 가이드 검증
공통 Checkbox에 가이드 전용 control·indicator 스타일을 적용했습니다. 모바일은 20px, PC는 16px로 반응형 크기를 제공하고, 미선택은 흰색 배경·회색 테두리, 선택은 GREEN700 배경·흰색 체크, focus-visible은 GREEN700 링, disabled는 회색 상태로 정리했습니다. 상담 폼의 전체동의·개별동의·수신 매체 라벨에 동일한 타이포그래피 토큰을 연결했고, 전체동의 체크박스의 pointerdown 전파 차단도 보완했습니다. 관련 테스트 11개, TypeScript·프로덕션 빌드 및 FO PC·모바일 화면 검증을 완료했습니다. 관리자 DB 인증 오류 로그는 기존 관리자 경로의 오류로 이번 FO 변경과 무관합니다.

## FO 상담 폼 요청사항·관심 서비스 변경 검증
관심 서비스 단일 Select를 Checkbox 기반 복수 선택 그룹으로 변경했습니다. 구내식당·간식·조식·사내카페·케이터링을 2열 그룹으로 표시하고, 선택값은 기존 API의 serviceType 문자열에 쉼표로 직렬화합니다. 요청사항 라벨을 ‘요청사항’으로 변경하고 Textarea에 mobile min-height 22.5rem, desktop min-height 18rem을 적용해 기존 기준 높이의 3배로 확장했습니다. 관련 회귀 테스트 14개, TypeScript·프로덕션 빌드와 FO PC·모바일 화면 검증을 완료했습니다. 관리자 페이지는 변경하지 않았습니다.

## 모바일 카드 캐러셀 레퍼런스 조사
- Material Design 3 Carousel: https://m3.material.io/components/carousel/overview
  모바일 캐러셀은 한 번에 일부 카드를 보여주고 다음 콘텐츠가 이어지는 구조로 탐색을 유도하는 대표 패턴입니다.
- Nielsen Norman Group, Carousels on Mobile Devices: https://www.nngroup.com/articles/mobile-carousels/
  캐러셀은 낮은 발견 가능성과 순차 접근 문제가 있으므로, 마지막 항목까지 3~4회 탭/스와이프로 도달할 수 있도록 구성하고, 많은 항목은 직접 접근 가능한 리스트가 더 적합하다고 안내합니다. 점(dot)만으로는 캐러셀임을 충분히 알리기 어려울 수 있어 다음 카드 peek, 좌우 버튼, 명확한 스와이프 affordance를 함께 고려합니다.
- FO 적용 추천: 현재 사내카페 모바일 가로 카드에는 스크롤바를 숨기고 `scroll-snap`을 유지하되, 카드 폭을 화면보다 작게 설정해 다음 카드가 12~20px 보이도록 합니다. 하단에는 얇은 진행 인디케이터 또는 카드 수 표시를 두고, 필요할 때만 좌우 화살표를 제공하는 방향이 브랜드와 사용성에 적합합니다.

## FO 모바일 식단·사내카페 Peek Carousel 검증
- 모바일 390px 전체 화면에서 기존 카드 비율과 이미지·본문·태그 구성은 유지되었습니다.
- 식단·사내카페 모바일 캐러셀 컨테이너는 `scroll-snap`과 다음 카드 일부 노출 폭을 사용하고 기본 스크롤바는 숨김 처리했습니다.
- PC 1280px 전체 화면에서는 기존 식단·사내카페 3열 카드 레이아웃을 유지하며 모바일 전용 변경이 데스크톱에 영향을 주지 않았습니다.
- 타입 검사와 FO 카드 회귀 테스트, 프로덕션 빌드는 통과했습니다. 관리자 API의 기존 DB 인증 오류 로그는 이번 FO 변경과 무관합니다.

## FO 식단 설명 텍스트 웹 편집 검증
- 웹 편집 대상은 JSX의 렌더링 태그가 아니라 `meals` 데이터의 `fullDescription`에 있어 초기 편집에서 대상 미탐색이 발생했습니다.
- 해당 데이터 정의를 직접 수정해 정통 한식 설명을 요청된 길이로 축약했습니다.
- Home 카드 회귀 테스트에 축약 문구 존재와 기존 문구 제거를 추가했습니다.
- 390px 모바일과 1280px 데스크톱 전체 화면에서 식단 카드, Peek Carousel, PC 3열 레이아웃의 회귀를 확인했습니다.

## FO 사내카페 설명 텍스트 웹 편집 검증
웹 편집 대상은 렌더링된 `<p>` 요소가 아니라 `Home.tsx`의 `meals` 데이터 `fullDescription` 정의에 있어 자동 편집에서 대상 미탐색이 발생했습니다. 해당 값을 요청된 길이로 수동 축약하고 Home 카드 회귀 테스트에 축약 문구와 이전 문구 제거를 고정했습니다. 390px 모바일 화면에서 상단 FO 레이아웃과 카드 진입 상태를, 1280px 데스크톱 전체 화면에서 사내카페 카드·기존 PC 레이아웃·전체 랜딩 흐름을 확인했으며 TypeScript와 프로덕션 빌드가 통과했습니다.

## FO 프리미엄 커피 설명 텍스트 웹 편집 검증
웹 편집 대상은 렌더링된 `<p>` 요소가 아니라 `Home.tsx`의 `meals` 데이터 `fullDescription` 정의에 있어 자동 편집에서 대상 미탐색이 발생했습니다. 요청된 문구를 데이터 정의에서 수동 축약하고 Home 카드 회귀 테스트에 축약 문구와 이전 문구 제거를 고정했습니다. 390px 모바일 전체 화면과 1280px 데스크톱 전체 화면에서 사내카페 카드·기존 Peek Carousel 및 랜딩 레이아웃 회귀를 확인했으며, 관련 FO 테스트 6개, TypeScript 검사, 프로덕션 빌드가 통과했습니다.

## FO 고객 성공사례 현장 사진 교체 검증
기존 고객 성공사례 카드의 DiceBear 캐릭터 이미지 6개를 프로젝트에 이미 연결되어 있던 실제 현장 사진 에셋 6개로 교체했습니다. 카드 수·회사 유형 텍스트·1:1 이미지 영역·object-fit cover·radius는 유지했습니다. 390px 모바일 전체 화면과 1280px 데스크톱 전체 화면에서 카드 표시와 랜딩 레이아웃을 확인했으며, 연결한 6개 `/manus-storage/` URL은 모두 HTTP 200 응답을 반환했습니다. 카드 회귀 테스트 7개, TypeScript 검사, 프로덕션 빌드가 통과했습니다. 관리자 화면은 변경하지 않았습니다.

## FO 고객 후기 모바일 6개 전체 노출 검증
모바일 고객 후기의 `reviews.slice(0, 3)` 제한을 제거하고 6개 전체를 동일한 가로형 카드로 세로 스택 렌더링했습니다. 390px 모바일 전체 화면에서 여섯 카드가 순서대로 스크롤 가능하고, 기존 1:1 현장 사진·텍스트·서비스 태그 구조가 유지되는 것을 확인했습니다. 1280px 데스크톱에서는 기존 6개·2열 레이아웃이 유지됩니다. 카드 회귀 테스트 8개, TypeScript 검사, 프로덕션 빌드가 통과했습니다.

## FO 배송·수거 안내 문구 수동 보정 검증
웹 편집에서 찾지 못한 프로세스 데이터의 배송·수거 설명을 `따뜻하게 배송하고 위생적으로 수거합니다`로 수정했습니다. 390px 모바일과 1280px 데스크톱 전체 화면에서 프로세스 카드 레이아웃과 문구 영역을 확인했으며, 카드 회귀 테스트 9개·TypeScript 검사·프로덕션 빌드가 통과했습니다.

## FO 푸터 개인정보 처리방침 링크 강조 검증
데스크톱 전체 페이지에서 최하단 개인정보 처리방침 버튼에 `font-bold`가 적용되어 이용약관보다 명확한 굵기로 표시되는 것을 확인했다. 모바일에서도 링크가 줄바꿈이나 레이아웃 충돌 없이 표시되며 기존 클릭·hover 동작을 유지한다. 푸터 회귀 테스트와 TypeScript·프로덕션 빌드를 통과했다. 전체 테스트의 관리자 DB 조회 실패 1건은 기존 외부 DB 인증 오류로 이번 푸터 변경과 무관하다.

## 운영 도메인 최신 배포본 점검
운영 도메인 `https://cjfreshwayfreshmealon.manus.space`는 HTTPS로 정상 접속되고 FO 기본 콘텐츠가 로드되었다. 다만 운영 배포본의 개인정보 처리방침 버튼 클래스는 `text-gray-400 hover:text-white transition text-sm`로 확인되어, 최신 체크포인트의 `font-bold` 변경이 아직 운영 배포본에는 반영되지 않은 상태다. 이번 변경은 새 체크포인트 저장까지 완료되었고, 실제 운영 반영은 관리 화면의 Publish 절차가 필요하다.

## FO 헤더 reduced-motion 접근성 검증
메인 헤더 타이틀 애니메이션에 `@media (prefers-reduced-motion: reduce)` 예외를 추가해 애니메이션을 제거하고 타이틀을 즉시 표시하도록 보완했다. CSS 회귀 테스트, TypeScript 검사, 프로덕션 빌드를 통과했다.

## FO 주요 브레이크포인트 검증
1280px 데스크톱, 768px 태블릿, 390px 모바일 전체 페이지에서 hero·3열 네비게이션·식단/카페 카드·고객 성공사례·하단 CTA·푸터의 반응형 흐름을 확인했다. 태블릿에서는 콘텐츠가 화면 폭에 맞춰 축소되고 수평 넘침이 발생하지 않았다. 전체 페이지 캡처 특성상 하단 lazy 이미지 일부는 초기 placeholder 상태로 보일 수 있으나 데스크톱·모바일 일반 렌더링에서 실제 에셋 연결을 기존 검증으로 확인했다.

## 게시된 운영본 반영 누락 점검
운영 도메인에서 랜딩 기본 콘텐츠와 푸터 개인정보 처리방침 버튼은 노출되지만, 푸터 버튼의 운영본 클래스는 최신 체크포인트와 달리 볼드체가 아닐 가능성이 확인되었다. 운영본에서 현재 viewport 기준 fixed/sticky 요소가 조회되지 않아 우측 하단 상담 위젯은 접힌 상태이거나 운영본 빌드에 포함되지 않은 것으로 추가 확인이 필요하다. 코드 최신본에는 위젯 동의 절차와 푸터 `font-bold`가 존재하므로, 게시본 버전 또는 배포 캐시 차이를 우선 점검한다.

## 운영본 상담 위젯 재확인
운영 도메인을 페이지 하단까지 스크롤한 뒤 맞춤 상담 버튼을 클릭하자 상담 위젯이 정상적으로 열리고, 이동급식·조식 서비스·간식 서비스·케이터링·사내카페 선택 단계가 노출되었다. 따라서 위젯 자체와 상담 플로우는 게시본에 포함되어 있다. 사용자가 확인한 동의 절차는 3단계 입력 화면까지 진행해야 보이는 구조이므로, 다음 단계에서 운영본 입력 흐름을 통해 동의 영역을 확인한다.

## 운영본 상담 위젯 동의 단계 확인
게시된 운영본에서 맞춤 상담 위젯을 서비스·환경 선택 후 입력 단계까지 진행했다. 이름·회사명·연락처·이메일·예상 식수·희망 지역·요청사항 입력란 아래에 `전체 동의`, `개인정보 수집 및 이용 동의`, `마케팅 정보 수신 동의 (선택)`, `광고성 정보 수신 동의 (선택)` 및 각 내용 열기 버튼이 실제로 노출되었다. 동의 절차는 게시본에 적용되어 있으며, 처음 위젯이 접힌 상태에서는 입력 단계까지 진행해야 확인할 수 있다.

## 운영본 동의·푸터 최종 DOM 확인
운영 도메인에서 상담 위젯 입력 단계의 네 동의 요소 ID(`chatbot-all-agree`, `chatbot-personal-info`, `chatbot-marketing`, `chatbot-advertising`)가 모두 존재하고, 전체 동의 클릭 후에도 내용 열기 버튼이 별도 유지되는 것을 확인했다. 같은 운영본의 개인정보 처리방침 버튼 클래스에는 `font-bold`가 포함되어 있어 게시된 버전에도 두 변경 사항이 반영되어 있다.

## 고객 특성 식단 모바일 캐러셀 구현 검증
고객 특성 식단 영역에 `data-diet-carousel`과 이전·다음 화살표의 접근성 라벨이 존재하고, 카드 인디케이터에도 각 식단명 라벨과 `aria-pressed` 상태를 추가했다. 데스크톱 미리보기에서는 기존 좌우 카드 레이아웃이 유지되고 화살표는 `md:hidden`으로 숨겨진다. 모바일 CSS에서는 이미지 래퍼가 `aspect-[4/3]`을 사용하므로 4:3 비율이 적용된다.

## 고객 특성 식단 4:3·화살표 시각 검증
변경 후 1280px 데스크톱 전체 화면에서 기존 식단 카드의 좌우 구성과 콘텐츠를 유지하고 모바일 전용 화살표가 노출되지 않는 것을 확인했다. 390px 모바일 전체 화면에서는 식단 카드가 세로 화면 폭에 맞는 4:3 이미지 영역을 사용하며 카드 아래 안내 문구가 유지된다. 모바일 전용 이전·다음 버튼은 코드상 이미지 중앙 좌우에 배치되고 키보드 포커스 링을 제공한다.

## 고객 특성 식단 카드 동일 높이·중앙 화살표 검증
참고 레이아웃 기준으로 모바일 카드의 이미지와 텍스트 영역 모두 `aspect-[4/3]`을 사용해 동일한 높이로 맞췄다. 화살표는 카드 전체 컨테이너의 상단 절반을 덮는 중앙 정렬 레이어로 이동해 이미지와 텍스트 경계에 해당하는 카드 중간 높이에 배치했다. 390px 모바일과 1280px 데스크톱 화면을 재확인했으며, 데스크톱에서는 모바일 전용 화살표가 숨겨지고 기존 좌우형 카드가 유지된다.

## 고객 특성 식단 카드 화살표 전체 중앙 보정 검증
참고 이미지 기준으로 모바일 화살표 부모를 카드 전체 높이(`inset-y-0`)에 맞춘 절대 위치 레이어로 변경했다. 이미지와 텍스트 영역은 각각 `aspect-[4/3]`과 동일한 패딩·최소 높이 규칙을 사용해 같은 높이로 정렬된다. 390px 모바일 및 1280px 데스크톱 전체 화면을 재확인했으며, 데스크톱 기존 카드 구조와 모바일 전용 화살표 숨김 상태가 유지된다.

## 고객 특성 식단 PC 4:3 비율 보정 검증
PC 이미지 래퍼와 텍스트 래퍼에서 `md:aspect-auto` 및 `md:min-h-96` 의존성을 제거하고 두 영역 모두 `aspect-[4/3]`으로 고정했다. 1280px PC 전체 화면에서 카드가 이미지와 텍스트가 좌우로 배치된 4:3 기준으로 정렬되며, 390px 모바일에서도 동일 높이·중앙 화살표·스와이프 구조가 유지되는 것을 확인했다.

## 상단 네비게이션 원복 및 스낵픽 전용 컬러 카테고리 검증
잘못 변경했던 상단 SectionNavigation은 59696b80 기준 기존 카드·단색 아이콘 스타일로 복원했다. 맞춤형 큐레이션 스낵픽에는 베이커리·샐러드·샌드위치·밥·라면·음료 6개만 남기고 컬러 아이콘과 텍스트 카드 UI를 적용했다. PC는 1×6, 모바일은 3열 그리드로 렌더링되며 상품 필터와 기존 상품 카드가 유지된다. 390px 모바일과 1280px PC 전체 화면에서 상단 원복 및 스낵픽 전용 변경을 확인했다.

## 스낵픽 B 시안 슬림 필터 칩 적용 검증
맞춤형 큐레이션 스낵픽의 카테고리 UI를 낮은 pill형 슬림 필터 칩으로 변경했다. 컬러 아이콘과 텍스트는 가로 정렬되며 선택 상태는 CJ 그린 배경과 흰색 아이콘·텍스트, 비선택 상태는 흰색 배경·얇은 테두리·카테고리별 아이콘 컬러로 표현된다. PC에서는 6개 한 줄, 모바일에서는 3열 2행으로 유지된다. 390px 모바일과 1280px PC 전체 화면에서 상단 서비스 네비게이션 원복, 스낵픽 칩 높이 축소, 상품 카드 회귀를 확인했다.

## 스낵픽 레퍼런스형 넓은 직사각형 칩 적용 검증
사용자 레퍼런스에 맞춰 스낵픽 카테고리를 pill이 아닌 넓은 직사각형 칩으로 변경했다. 베이커리·샐러드·샌드위치·밥·라면·음료 아이콘과 텍스트를 가로로 배치하고, 선택 항목만 CJ 그린 배경과 흰색 아이콘·텍스트로 강조했다. PC와 모바일 모두 3열×2행이며 PC 칩 높이는 96px, 모바일 칩 높이는 72px로 설정했다. 390px 모바일과 1280px PC 전체 화면에서 상품 필터·상단 네비게이션·상품 카드 회귀를 확인했다.

## 스낵픽 레퍼런스형 칩 높이 절반 축소 검증
스낵픽 카테고리 칩의 높이를 PC 96px에서 48px(`md:min-h-12`), 모바일 72px에서 40px(`min-h-10`)로 줄이고 아이콘도 PC 28px·모바일 20px로 축소했다. 컬러 아이콘·텍스트 가로 배치, 선택·hover·focus 상태, PC·모바일 3열×2행과 상품 영역을 유지했다. 390px 모바일과 1280px PC 전체 화면에서 칩이 과도하게 크지 않고 상품 카드와 균형을 이루는지 확인했다.

## 상단 모바일 서비스 네비게이션 정사각형 타일 검증
상단 서비스 네비게이션의 모바일 버튼에 `aspect-square`와 `w-full`을 적용해 6개 타일을 3열×2행 정사각형으로 고정했다. 아이콘·텍스트 중앙 정렬, 활성 CJ 그린 상태, 기존 앵커 이동은 유지하고 PC의 1×6 가로형 레이아웃에는 변경을 적용하지 않았다. 390px 모바일과 1280px PC 전체 화면에서 모바일 정사각형 타일과 PC 회귀를 확인했다.

## 상담신청 폼 FW Radio button 가이드 적용 검증
첨부된 FW UIUX Design System의 원형 라디오 버튼 시각을 상담신청 폼 선택 컨트롤에 적용했다. 공통 Checkbox에 `appearance="radio"` 변형을 추가해 기존 checkbox 의미와 다중 선택 동작은 유지하면서, 흰색 바탕·회색 테두리·CJ 그린 선택 링과 중앙 선택점·focus 링·disabled 상태를 제공한다. 관심 서비스 5개, 전체 동의, 개별 동의 3개, 광고 수신 매체 3개에 적용했으며 동의 아코디언과 전체 동의 이벤트 분리도 유지했다. 상담 폼·CSS 회귀 테스트, TypeScript, 프로덕션 빌드를 통과했다.

## 상담신청 폼 helper 문구 제거 검증
상담신청 폼의 회사명·담당자명·연락처·이메일·관심 서비스·지역·예상 인원·요청사항 아래에 있던 평상시 helper 문구를 제거했다. 오류 상태에서만 `form-field-error`가 표시되도록 aria-describedby를 오류 메시지 ID에 조건부 연결했고, 입력 placeholder·필수 검증·동의 영역은 유지했다. FO 모바일·PC 전체 화면 회귀와 관련 테스트·TypeScript·프로덕션 빌드를 확인했다.

## 상담신청 폼 관심 서비스 FW Checkbox 적용 검증
관심 서비스 5개 항목은 FW Checkbox 가이드에 맞춘 사각형 체크박스로 분리 적용했다. 미선택은 흰색 바탕·회색 사각 테두리, 선택은 CJ 그린 배경·흰색 체크, focus 링과 disabled 상태를 사용한다. 관심 서비스는 checkbox 의미와 복수 선택을 유지하고, 전체 동의·개별 동의·광고 수신 매체는 앞서 적용한 원형 Radio appearance를 유지했다. helper 문구 제거, 동의 아코디언 이벤트 분리, 상담 폼 관련 회귀 테스트·TypeScript·프로덕션 빌드 및 FO 모바일 기본 레이아웃을 확인했다.

## 하단 CTA 상담 버튼 단독 노출·반전 색상 검증
하단 그린 CTA에서 `자료 다운받기` 버튼을 제거하고 `지금 상담받기` 버튼만 남겼다. 하단 버튼은 `on-brand-inverse` variant로 흰색 배경·CJ 그린 텍스트·흰색 테두리를 사용하며 hover 시 CJ 그린 계열 배경과 흰색 텍스트로 전환된다. active 축소·focus 링·disabled 상태는 공통 버튼 규칙을 유지한다. 상단 히어로의 상담·자료 다운로드 두 버튼은 유지했으며, PC·모바일 전체 화면에서 하단 CTA의 단독 버튼과 대비를 확인했다.

## 메인 배너 상담 버튼 단독 노출 검증
메인 히어로 배너에서 자료 다운받기 버튼을 제거하고 지금 상담받기 버튼만 남겼다. 상담 버튼은 기존 `on-brand` 스타일과 상담 모달 연결을 유지하며, 하단 CTA에는 이미 적용된 `on-brand-inverse` 스타일을 유지한다. 페이지 내 자료 다운로드 이벤트는 제거되었고, 메인·중간 견적 문의·하단 상담 이벤트 연결과 PC·모바일 히어로 화면을 회귀 확인했다. CTA 회귀 테스트·버튼 테스트·TypeScript·프로덕션 빌드를 통과했다.

## 메인 배너 왼쪽 정렬 검증
메인 히어로의 모바일·PC 제목, 설명, 상담 버튼을 왼쪽 정렬로 변경했다. 배경 영상과 하단 슬라이드 인디케이터는 중앙 위치를 유지하고, 상담 버튼은 단독 노출·기존 상담 모달 이벤트 연결을 보존했다. 390px 모바일과 1280px PC 화면에서 텍스트 줄바꿈·버튼 위치·가독성을 확인했으며 CTA 회귀 테스트, TypeScript, 프로덕션 빌드를 통과했다.

## 스낵픽 메인 헤더 3줄·가독성 확대 검증
스낵픽 히어로의 제목 데이터를 ‘어디든지 찾아가는’과 ‘간식복지\\n스낵픽’으로 분리해 시각적으로 3줄 계층을 만들었다. 스낵픽 슬라이드에만 모바일·PC 확대 폰트와 `white-space: pre-line`을 적용하고, 다른 히어로 슬라이드는 기존 크기를 유지하도록 `isSnackHero` 조건을 사용했다. 왼쪽 정렬·상담 버튼·배경 영상·인디케이터를 보존했으며 CTA 회귀 테스트·TypeScript·프로덕션 빌드를 통과했다.

## 메인 헤더 타이틀 중앙 정렬 검증
메인 히어로의 제목과 강조 문구에만 `text-center`를 적용해 PC·모바일에서 중앙 정렬을 확인했다. 설명 문구와 지금 상담받기 버튼은 기존 `text-left`를 유지했으며, 배경 영상·인디케이터·상담 모달 연결도 보존했다. 390px 모바일과 1280px PC 스크린샷에서 타이틀 중앙 정렬 및 배너 레이아웃을 확인했고 CTA 회귀 테스트·TypeScript·프로덕션 빌드를 통과했다.

## 메인 배너 전체 콘텐츠 중앙 정렬 검증
메인 히어로의 타이틀·강조 문구뿐 아니라 모바일·PC 서브 설명과 지금 상담받기 버튼에도 중앙 정렬을 적용했다. 배경 영상·인디케이터·상담 모달 연결은 유지했으며, 390px 모바일과 1280px PC 스크린샷에서 콘텐츠 중앙 배치와 버튼 위치를 확인했다. CTA 회귀 테스트·TypeScript·프로덕션 빌드를 통과했다.

## 메인 타이틀 PC·모바일 고정 규격 검증
메인 배너 타이틀을 모바일 `30px`, PC `60px`로 고정했다. 모바일에서는 제목·강조 문구가 3줄로 보이고, PC에서는 제목과 강조 문구가 2줄 구조로 보이도록 PC 강조 문구의 줄바꿈을 정상 공백으로 처리했다. 타이틀·서브 설명·상담 버튼 중앙 정렬, 배경 영상·인디케이터·상담 모달 연결을 유지했으며 390px·1280px 시각 검증과 CTA 회귀 테스트·TypeScript·프로덕션 빌드를 통과했다.

## 모바일 히어로 슬라이드별 3줄 타이틀·왼쪽 정렬 검증
모바일 히어로 타이틀을 프레시밀온은 `어디든지 찾아가는 / 사내복지 / 프레시밀온`, 스낵픽은 `직원 취향 저격하는 / 맞춤 큐레이션 / 스낵픽`, 카페프레시는 `기업 문화를 만드는 / 사내카페 / 카페프레시`로 분리했다. 모바일 타이틀·서브 설명·상담 버튼은 왼쪽 정렬로 변경했고, PC는 60px·2줄·중앙 정렬을 유지했다. 390px 모바일·1280px PC 시각 검증과 CTA 회귀 테스트·TypeScript·프로덕션 빌드를 통과했다.
