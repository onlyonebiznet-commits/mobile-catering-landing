
## 2026-08-27 후속 이미지·필터 작업 시각 검증
- 데스크톱 `/`: 메인 배너의 상담받기·자료 다운받기 버튼이 동일한 흰색 On-brand 스타일로 렌더링됨.
- 모바일 `/` (375×812): 두 CTA가 동일한 높이·radius·색상 체계로 가운데 정렬되어 표시되며, 제목·설명·네비게이션이 기존 반응형 레이아웃을 유지함.
- `/admin`: 관리자 로그인 화면 정상 렌더링 확인.
- 기간 필터는 로그인 이후 대시보드에서 날짜 입력 2개와 기존 검색·상태·서비스·정렬 필터가 함께 표시되도록 구현됨.
- 개발 서버 TypeScript 상태는 오류 없음. 콘솔에는 기존 DB 인증 오류 로그가 남아 있으나 프론트엔드 컴파일 오류는 확인되지 않음.

## CTA 기본 GREEN700·hover 화이트 검증
- 데스크톱 홈페이지 hero에서 상담받기·자료 다운받기 버튼이 모두 GREEN700 계열 배경으로 표시됨.
- 모바일 홈페이지 hero에서도 두 버튼이 동일한 크기·간격·반응형 정렬로 표시됨.
- hover 상태는 공통 `on-brand` variant의 `hover:bg-white hover:border-primary hover:text-primary` 클래스와 버튼 회귀 테스트로 검증함.

## 스낵픽 실패 생성 에셋 점검
- 저장소 응답을 확인한 결과 여러 `snackpick-*.jpg` 경로가 실제 이미지가 아니라 `Image generation failed` SVG(498 bytes)를 반환함.
- 유효한 기존 에셋으로 확인된 `pasted_file_5hIjDd_image_ccf1f4d9.png`는 포장 간식 제품 이미지이고, `cropped_cafe_IMG_6844_dd3c1ce0.jpg`는 카페 운영 사진임.
- 실패한 이미지 경로는 실제 유효 에셋으로 교체하기 전까지 완료 처리하지 않음.

## 유효 이미지 후보 추가 검토
- `snackpick-ramen-reference_d9f27046.jpg`는 1920×1920 정사각형 라면 제품 사진으로 카드용 대체 에셋으로 사용 가능함.
- `snackpick-solution_634d0e45.webp`는 스낵픽 진열·사내 공간 운영 사진으로 섹션 대표 이미지에 적합함.

## 스낵픽 최종 에셋 연결 확인
- 실패 SVG를 반환하던 생성 경로는 Home.tsx의 상품 데이터에서 제거함.
- 현재 연결된 고유 에셋은 라면 정사각형 제품 사진, 포장 간식 이미지, 카페 운영 사진, 핑거푸드 사진이며 모두 저장소에서 HTTP 200과 이미지 콘텐츠로 응답함.
- 남은 `snackpick-solution_634d0e45.webp`는 스낵픽 운영 공간 대표 이미지로 유효함.

## 스낵픽 PC·모바일 시각 검증
- 데스크톱 전체 페이지에서 스낵픽 상품 카드가 깨진 실패 SVG 없이 정사각형 이미지 카드로 표시됨.
- 모바일 전체 페이지에서도 카테고리 4×2 그리드와 상품 카드가 정상적으로 쌓이며, 교체된 이미지가 렌더링됨.
- 고객 성공사례·하단 CTA·푸터까지 기존 반응형 흐름을 유지함.

## 검색 접근성 기본 파일 검증
`client/public/robots.txt`를 추가해 공개 랜딩 경로는 허용하고 `/admin`, `/api` 경로는 검색 크롤링에서 제외했습니다. 정적 파일 존재 여부, 관리자 경로 차단 지시문, SEO 테스트, TypeScript 검사, 프로덕션 빌드를 확인했습니다.

## 최신 런타임 점검
최근 브라우저 콘솔에서 error·uncaught·failed 항목이 발견되지 않았고, 네트워크 로그에도 4xx·5xx 실패 요청이 없었습니다. 공통 `on-brand` variant는 기본 `bg-primary`와 `text-primary-foreground`를 사용하고 hover 시 `bg-white`, `border-primary`, `text-primary`로 전환됩니다.

## 관리자 기간 필터 반응형 확인
모바일 `/admin/login`과 `/admin/dashboard` 경로를 확인했습니다. 인증되지 않은 `/admin/dashboard` 접근은 관리자 로그인 화면으로 안전하게 리다이렉트되므로, 실제 기간 필터 화면의 모바일 렌더링은 관리자 세션 없이는 시각 검증할 수 없습니다. 기간 필터의 반응형 클래스와 입력 제약은 소스 및 타입 검사로 확인했지만, 인증 후 브라우저 검증 항목은 보류합니다.

## FO 전용 전체 화면 검증
관리자 화면은 제외하고 FO `/` 랜딩페이지를 1280px 데스크톱과 390px 모바일 전체 길이로 확인했습니다. 메인·하단 CTA는 배너 중앙에 배치되고, 모바일에서는 두 CTA가 함께 보이는 컴팩트한 가로 배치가 유지됩니다. 스낵픽 상품 이미지와 카테고리 영역, 고객 성공사례·푸터까지 수평 넘침 없이 렌더링되었습니다. 관리자 기간 필터는 이번 검증 범위에서 제외했습니다.

## 모바일 네비게이션 3열 검증
모바일 FO 네비게이션을 `grid-cols-3`으로 변경해 6개 메뉴가 첫 행 3개·둘째 행 3개로 정렬되는 것을 확인했습니다. 기존 4개·2개 불균형 배치가 제거되었고 타일의 아이콘·텍스트·활성 상태는 유지됩니다. 데스크톱에서는 기존 `grid-cols-6` 구조와 CTA·Hero 레이아웃이 그대로 유지됩니다.

## FO 상담·자료 신청 폼 Text input 가이드 검증
상담받기와 자료 다운받기 CTA가 공통으로 여는 `ConsultationModal`의 입력 요소를 동일한 Text input 토큰으로 정리했습니다. 라벨·필수 표시·helper·오류 메시지의 계층과 `aria-describedby` 연결을 적용했고, Input·Textarea·SelectTrigger에 모바일/데스크톱 크기, 기본·focus·disabled·success·error 상태 토큰을 반영했습니다. 활성 FO 폼의 이전 인라인 입력 스타일 잔여 여부를 정적 검색으로 확인했으며, FO 테스트와 프로덕션 빌드를 통과했습니다.

## Text input 변경 후 FO 반응형 회귀
Text input 가이드 토큰 반영 이후 FO 데스크톱(1280px)과 모바일(375px) 루트 화면을 확인했습니다. 메인 배너 텍스트·CTA 정렬, 모바일 3열 네비게이션의 상단 노출, 기존 hero 레이아웃이 깨지지 않았습니다. 상담 폼 자체의 입력 상태는 관련 컴포넌트 테스트·타입 검사·프로덕션 빌드로 검증했습니다.

## Badge 가이드 적용 후 FO 반응형 검증
Badge 컴포넌트에 StatusBadge의 중립·긍정·주의·경고 상태와 PromoBadge의 filled/outlined 색상 프리셋, large/medium/small 크기를 추가했습니다. FO 카드의 기존 인라인 태그는 PromoBadge green outlined small로 교체했습니다. 데스크톱과 모바일 루트 화면에서 hero·CTA·네비게이션 레이아웃이 유지되는 것을 확인했으며, Badge·폼 관련 회귀 테스트와 TypeScript·프로덕션 빌드를 통과했습니다.

## PromoBadge 예시 스타일 적용 검증
PromoBadge outlined를 첨부 예시처럼 둥근 사각형(`rounded-md`)·화이트 배경·옐로 상태 테두리·짙은 텍스트 조합으로 변경하고, FO 카드 태그의 색상을 yellow outlined small로 적용했습니다. PC와 모바일 전체 페이지 캡처에서 스낵픽·식단·사내카페 태그가 동일한 외형으로 표시되고 카드 레이아웃이 유지되는 것을 확인했습니다.

## FO PromoBadge filled pill 재적용 검증
첨부 예시 기반으로 FO 카드 태그를 `yellow`·`filled`·`small` PromoBadge로 전환했습니다. `#FF9700` 경고색 단색 배경, 짙은 텍스트, 테두리 없는 `rounded-full` pill 형태가 적용됩니다. PC·모바일 전체 화면에서 식단·사내카페·키친리스 카드 태그의 외형과 카드 레이아웃을 확인했으며, Badge·폼 회귀 테스트 13개, TypeScript와 프로덕션 빌드를 통과했습니다.

## FO PromoBadge 최신 아웃라인 복원 검증
최신 첨부 예시에 맞춰 FO 카드 태그를 yellow outlined small로 복원했습니다. 화이트 배경, `#FF9700` 얇은 테두리, 짙은 텍스트, `rounded-md` 둥근 사각형을 유지합니다. PC·모바일 전체 화면에서 식단·사내카페·키친리스 카드의 태그 표시와 기존 카드 레이아웃을 확인했으며, Badge·폼 회귀 테스트 13개, TypeScript·프로덕션 빌드를 통과했습니다.

## FO 식단·사내카페 카드 배경 흰색 통일 검증
식단과 사내카페 카드의 `bg-gray-50` 본체 배경을 키친리스 밀솔루션 카드와 같은 `bg-white`로 변경했습니다. 모바일 카드 내부 이미지 영역의 `bg-gray-100`도 `bg-white`로 통일해 카드 전체의 배경 대비를 맞췄습니다. PC·모바일 전체 화면에서 카드 본체와 이미지 영역이 흰색으로 표시되고 태그·그림자·레이아웃이 유지되는 것을 확인했습니다. 관련 회귀 테스트 15개, TypeScript와 프로덕션 빌드를 통과했습니다.

## FO 상담 신청 폼 Checkbox 가이드 검증
공통 Checkbox에 가이드 전용 control·indicator 스타일을 적용했습니다. 모바일은 20px, PC는 16px로 반응형 크기를 제공하고, 미선택은 흰색 배경·회색 테두리, 선택은 GREEN700 배경·흰색 체크, focus-visible은 GREEN700 링, disabled는 회색 상태로 정리했습니다. 상담 폼의 전체동의·개별동의·수신 매체 라벨에 동일한 타이포그래피 토큰을 연결했고, 전체동의 체크박스의 pointerdown 전파 차단도 보완했습니다. 관련 테스트 11개, TypeScript·프로덕션 빌드 및 FO PC·모바일 화면 검증을 완료했습니다. 관리자 DB 인증 오류 로그는 기존 관리자 경로의 오류로 이번 FO 변경과 무관합니다.

## FO 상담 폼 요청사항·관심 서비스 변경 검증
관심 서비스 단일 Select를 Checkbox 기반 복수 선택 그룹으로 변경했습니다. 구내식당·간식·조식·사내카페·케이터링을 2열 그룹으로 표시하고, 선택값은 기존 API의 serviceType 문자열에 쉼표로 직렬화합니다. 요청사항 라벨을 ‘요청사항’으로 변경하고 Textarea에 mobile min-height 22.5rem, desktop min-height 18rem을 적용해 기존 기준 높이의 3배로 확장했습니다. 관련 회귀 테스트 14개, TypeScript·프로덕션 빌드와 FO PC·모바일 화면 검증을 완료했습니다. 관리자 페이지는 변경하지 않았습니다.

## 모바일 카드 캐러셀 레퍼런스 조사
- Material Design 3 Carousel: https://m3.material.io/components/carousel/overview
  모바일 캐러셀은 한 번에 일부 카드를 보여주고 다음 콘텐츠가 이어지는 구조로 탐색을 유도하는 대표 패턴입니다.
- Nielsen Norman Group, Carousels on Mobile Devices: https://www.nngroup.com/articles/mobile-carousels/
  캐러셀은 낮은 발견 가능성과 순차 접근 문제가 있으므로, 마지막 항목까지 3~4회 탭/스와이프로 도달할 수 있도록 구성하고, 많은 항목은 직접 접근 가능한 리스트가 더 적합하다고 안내합니다. 점(dot)만으로는 캐러셀임을 충분히 알리기 어려울 수 있어 다음 카드 peek, 좌우 버튼, 명확한 스와이프 affordance를 함께 고려합니다.
- FO 적용 추천: 현재 사내카페 모바일 가로 카드에는 스크롤바를 숨기고 `scroll-snap`을 유지하되, 카드 폭을 화면보다 작게 설정해 다음 카드가 12~20px 보이도록 합니다. 하단에는 얇은 진행 인디케이터 또는 카드 수 표시를 두고, 필요할 때만 좌우 화살표를 제공하는 방향이 브랜드와 사용성에 적합합니다.

## FO 모바일 식단·사내카페 Peek Carousel 검증
- 모바일 390px 전체 화면에서 기존 카드 비율과 이미지·본문·태그 구성은 유지되었습니다.
- 식단·사내카페 모바일 캐러셀 컨테이너는 `scroll-snap`과 다음 카드 일부 노출 폭을 사용하고 기본 스크롤바는 숨김 처리했습니다.
- PC 1280px 전체 화면에서는 기존 식단·사내카페 3열 카드 레이아웃을 유지하며 모바일 전용 변경이 데스크톱에 영향을 주지 않았습니다.
- 타입 검사와 FO 카드 회귀 테스트, 프로덕션 빌드는 통과했습니다. 관리자 API의 기존 DB 인증 오류 로그는 이번 FO 변경과 무관합니다.

## FO 식단 설명 텍스트 웹 편집 검증
- 웹 편집 대상은 JSX의 렌더링 태그가 아니라 `meals` 데이터의 `fullDescription`에 있어 초기 편집에서 대상 미탐색이 발생했습니다.
- 해당 데이터 정의를 직접 수정해 정통 한식 설명을 요청된 길이로 축약했습니다.
- Home 카드 회귀 테스트에 축약 문구 존재와 기존 문구 제거를 추가했습니다.
- 390px 모바일과 1280px 데스크톱 전체 화면에서 식단 카드, Peek Carousel, PC 3열 레이아웃의 회귀를 확인했습니다.

## FO 사내카페 설명 텍스트 웹 편집 검증
웹 편집 대상은 렌더링된 `<p>` 요소가 아니라 `Home.tsx`의 `meals` 데이터 `fullDescription` 정의에 있어 자동 편집에서 대상 미탐색이 발생했습니다. 해당 값을 요청된 길이로 수동 축약하고 Home 카드 회귀 테스트에 축약 문구와 이전 문구 제거를 고정했습니다. 390px 모바일 화면에서 상단 FO 레이아웃과 카드 진입 상태를, 1280px 데스크톱 전체 화면에서 사내카페 카드·기존 PC 레이아웃·전체 랜딩 흐름을 확인했으며 TypeScript와 프로덕션 빌드가 통과했습니다.

## FO 프리미엄 커피 설명 텍스트 웹 편집 검증
웹 편집 대상은 렌더링된 `<p>` 요소가 아니라 `Home.tsx`의 `meals` 데이터 `fullDescription` 정의에 있어 자동 편집에서 대상 미탐색이 발생했습니다. 요청된 문구를 데이터 정의에서 수동 축약하고 Home 카드 회귀 테스트에 축약 문구와 이전 문구 제거를 고정했습니다. 390px 모바일 전체 화면과 1280px 데스크톱 전체 화면에서 사내카페 카드·기존 Peek Carousel 및 랜딩 레이아웃 회귀를 확인했으며, 관련 FO 테스트 6개, TypeScript 검사, 프로덕션 빌드가 통과했습니다.

## FO 고객 성공사례 현장 사진 교체 검증
기존 고객 성공사례 카드의 DiceBear 캐릭터 이미지 6개를 프로젝트에 이미 연결되어 있던 실제 현장 사진 에셋 6개로 교체했습니다. 카드 수·회사 유형 텍스트·1:1 이미지 영역·object-fit cover·radius는 유지했습니다. 390px 모바일 전체 화면과 1280px 데스크톱 전체 화면에서 카드 표시와 랜딩 레이아웃을 확인했으며, 연결한 6개 `/manus-storage/` URL은 모두 HTTP 200 응답을 반환했습니다. 카드 회귀 테스트 7개, TypeScript 검사, 프로덕션 빌드가 통과했습니다. 관리자 화면은 변경하지 않았습니다.

## FO 고객 후기 모바일 6개 전체 노출 검증
모바일 고객 후기의 `reviews.slice(0, 3)` 제한을 제거하고 6개 전체를 동일한 가로형 카드로 세로 스택 렌더링했습니다. 390px 모바일 전체 화면에서 여섯 카드가 순서대로 스크롤 가능하고, 기존 1:1 현장 사진·텍스트·서비스 태그 구조가 유지되는 것을 확인했습니다. 1280px 데스크톱에서는 기존 6개·2열 레이아웃이 유지됩니다. 카드 회귀 테스트 8개, TypeScript 검사, 프로덕션 빌드가 통과했습니다.

## FO 배송·수거 안내 문구 수동 보정 검증
웹 편집에서 찾지 못한 프로세스 데이터의 배송·수거 설명을 `따뜻하게 배송하고 위생적으로 수거합니다`로 수정했습니다. 390px 모바일과 1280px 데스크톱 전체 화면에서 프로세스 카드 레이아웃과 문구 영역을 확인했으며, 카드 회귀 테스트 9개·TypeScript 검사·프로덕션 빌드가 통과했습니다.
