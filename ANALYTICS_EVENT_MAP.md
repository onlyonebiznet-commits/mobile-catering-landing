# Analytics Event Map

## 앱에서 구현된 이벤트

| 사용자 행동 | 앱 이벤트/데이터 속성 | 호출 범위 |
|---|---|---|
| 메인·섹션 상담 CTA 클릭 | `data-event="consultation_click"` | 랜딩 페이지 CTA |
| 상담 신청 제출 | `consultation_submit` | 상담폼 제출 성공 흐름 |
| 자료 신청 제출 | `material_request_submit` | 자료 신청 제출 흐름 |
| 페이지 스크롤 | `scroll_depth` | `useGTMScrollTracking` 임계값 |
| 일반 클릭 | `trackGTMClick` / `data-event` | GTM 유틸리티와 주요 CTA |

## 현재 적용 범위

현재 프로젝트에는 GTM 스니펫, dataLayer 초기화, 클릭·제출·스크롤 이벤트 유틸리티와 회귀 테스트가 포함되어 있습니다. 동영상 섹션은 이전 요구사항에 따라 제거되어 현재 재생 대상이 없으므로 동영상 재생 이벤트는 **N/A**입니다.

## 외부 계정에서 필요한 확인

GA4 태그·키 이벤트(전환) 설정, GTM Preview 또는 운영 컨테이너에서 이벤트 수신 확인, Meta Pixel·Microsoft Clarity 연결, 사용자 정보 동기화는 운영 계정 권한과 컨테이너 설정이 필요합니다. 이 문서는 앱 코드의 이벤트명과 외부 설정의 매핑 기준을 제공하며, 계정 검증 전에는 완료로 표시하지 않습니다.

개인정보 보호를 위해 회사명·지역·예상 식수 등 상담 입력값을 분석 이벤트에 자동 포함하지 않으며, 운영 담당자가 별도 데이터 전송을 추가할 경우 개인정보 처리 기준을 먼저 검토해야 합니다.


## 개인정보 최소화 검증

상담 신청 성공 시 전송되는 GTM `form_submit` payload에는 서비스 유형·지역·예상 식수·폼 유형만 포함하며, 회사명·담당자명·연락처·이메일·요청사항은 포함하지 않습니다. 상담 API 자체에는 필요한 상담 정보가 전달되지만 분석 이벤트와 분리됩니다. 이 범위는 회귀 테스트로 고정했고 전체 테스트·TypeScript·프로덕션 빌드를 통과했습니다.


자료 신청 성공 이벤트도 직접 식별 정보 없이 `form_type: "material_request"`만 전송하도록 정리했으며, 자료 신청 모달 전용 회귀 테스트를 추가했습니다.
